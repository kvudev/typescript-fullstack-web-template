Add CI/CD pipeline to the project.

Package the project using Docker to ensure consistency across different environments. Set up a CI/CD pipeline using GitHub Actions to automate the build, test, and deployment processes. The pipeline should include steps for building the Docker image, running tests, and deploying the application to a staging environment for further testing before production deployment.
