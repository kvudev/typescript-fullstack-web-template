{
  "name": "@news-summarizer/db",
  "version": "1.0.0",
  "description": "Database module — Prisma repositories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0"
  },
  "devDependencies": {
    "prisma": "^5.13.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "@news-summarizer/layer": "workspace:*"
  }
}
